# status.yaml — DEPRECATED
#
# Project status is now tracked via the Agent Workbench API.
# Use the awb CLI instead of editing this file:
#
#   awb status show                          # view current status
#   awb status create --status active ...    # create a new record
#   awb status update <id> --version <n> ... # update existing record
#
# This file is kept as a last-resort fallback only, for when
# awb-api.taylor.lan is unreachable. Do not update it in normal operation.

status: "active"
phase: "review"
reason: "Deprecated — authoritative status is in awb-api.taylor.lan. See: awb status show --project agent-workbench"
updated: "2026-05-28"
